# 连续尺度估计用于自适应滑窗（SwinIR鲁棒版工程方案）

## 概述

本项目用于显微图像（如石墨烯畴区光学图像）的**无监督连续尺度估计系统**。  
目标是在**不使用比例尺、不使用倍率标签、不依赖实例分割**的情况下，自动估计图像的“视觉尺度”，并用于确定滑窗大小。

系统对 **SwinIR超分辨率图像具有鲁棒性**。

---

## 核心思想

不再预测倍率（10x/50x），而是学习：

```
图像 → 视觉特征 → 连续尺度 s → 滑窗大小 W
```

避免：
- SwinIR伪造高频纹理
- 不同显微镜设备差异
- 图像裁剪/缩放影响

---

## 系统流程

```
输入图像
   ↓
统一缩放（长边1024，保持长宽比）
   ↓
DINOv2特征提取
   ↓
PCA降维（768 → 64）
   ↓
尺度估计（kNN / 回归）
   ↓
连续尺度 s
   ↓
滑窗函数映射
   ↓
输出窗口大小 W
```

---

## 1. 特征提取

使用自监督视觉模型：

- DINOv2（推荐）

输出：
- 768维全局特征向量

```python
embedding = dinov2_model(image)
```

---

## 2. 降维处理

减少噪声，提高稳定性：

```python
from sklearn.decomposition import PCA

pca = PCA(n_components=64)
X_reduced = pca.fit_transform(X_embeddings)
```

---

## 3. 连续尺度估计

### 方法A：kNN回归（推荐）

构建数据库：

```
(embedding_i, scale_proxy_i)
```

推理：

```python
import numpy as np
from sklearn.neighbors import NearestNeighbors

def predict_scale(x, X_db, s_db, k=5):
    nbrs = NearestNeighbors(n_neighbors=k).fit(X_db)
    dist, idx = nbrs.kneighbors([x])

    weights = np.exp(-dist[0])
    weights /= weights.sum()

    return np.sum(weights * s_db[idx[0]])
```

---

### 方法B：MLP回归

```python
import torch.nn as nn

model = nn.Sequential(
    nn.Linear(64, 128),
    nn.ReLU(),
    nn.Linear(128, 1)
)
```

---

## 4. 无监督尺度库构建

无需真实倍率标签。

### 方法1：聚类生成伪尺度

```
HDBSCAN / KMeans
```

将 cluster_id 映射为 scale proxy。

---

### 方法2：频率代理（可选）

使用低频能量或结构尺度作为 proxy。

---

## 5. 连续滑窗映射

将尺度 s 转换为窗口大小 W：

```python
import numpy as np

def scale_to_window(s, Wmin=512, Wmax=1024, alpha=800, beta=1.2):
    W = alpha * (s ** (-beta))
    return int(np.clip(W, Wmin, Wmax))
```

---

## 6. SwinIR鲁棒性说明

该方法鲁棒原因：

- DINOv2不依赖高频纹理
- SwinIR主要改变局部细节
- embedding保留全局结构

增强策略：

### 多视图平均

```python
imgs = [original, downsampled, blurred]
s = mean([predict_scale(img) for img in imgs])
```

---

## 7. 推理流程

```python
def inference(image):
    img = resize(image, 224)

    emb = dinov2(img)
    emb = pca.transform(emb)

    s = knn_predict(emb)

    W = scale_to_window(s)

    return s, W
```

---

## 8. 与滑窗分割系统集成

### 传统方式：

```
window = 固定值（512/1024）
```

### 本方法：

```
window = f(image)
```

用于：
- Mask R-CNN滑窗推理
- 大图分割
- 畴区统计

---

## 9. 优势总结

- 无需倍率标签
- 无需比例尺
- 对SwinIR鲁棒
- 连续输出（非离散分类）
- 可直接用于工程系统
- 可扩展大规模数据

---

## 10. 可扩展方向

- FAISS加速最近邻
- UMAP尺度可视化
- 多尺度DINO融合
- 时序显微视频一致性约束

---

## 总结

该方法将传统“倍率分类问题”转化为：

> **连续视觉尺度流形学习问题**

更稳定、更鲁棒、更适合真实显微图像工程系统。
