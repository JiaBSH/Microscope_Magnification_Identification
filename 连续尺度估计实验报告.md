# 连续尺度估计用于自适应滑窗系统实验报告

## 1. 项目目标

本项目面向显微图像场景，目标是在不依赖比例尺、不直接使用倍率标签、不依赖实例分割结果的前提下，学习图像的连续视觉尺度，并将其映射为后续滑窗分割所需的窗口大小。

与传统的离散倍率分类不同，本方法将问题定义为：

$$
I \rightarrow f(I) \rightarrow s \rightarrow W
$$

其中：

- $I$ 表示输入图像。
- $f(I)$ 表示图像的全局视觉表征。
- $s$ 表示连续尺度值。
- $W$ 表示最终滑窗大小。

该设计的核心优势是将“倍率类别判断”转化为“连续视觉尺度估计”，更适合真实工程中的设备差异、裁剪差异、缩放差异以及超分辨率重建带来的纹理扰动。

## 2. 算法原理

### 2.1 连续尺度建模思想

传统方案通常直接预测 `2.5x`、`5x`、`20x`、`50x`、`100x` 等离散标签。这种做法的问题在于：

- 类间边界是人为设定的，不一定反映图像真实视觉结构差异。
- 图像经过超分辨率、缩放、裁剪后，离散标签容易失稳。
- 工程系统最终需要的是合适的窗口大小，而不是倍率标签本身。

因此，本项目采用连续尺度变量 $s$ 作为中间表示。系统不直接把问题约束为分类任务，而是先构造视觉嵌入，再从嵌入空间中估计样本在尺度流形中的相对位置，最后再用映射函数得到窗口尺寸。

### 2.2 特征表示

项目当前支持两种特征提取方式：

1. `robust_handcrafted`
   这是一个轻量级基线特征提取器，通过灰度直方图、梯度统计、低频/频域统计和粗粒度空间池化构造鲁棒特征。优点是依赖少、容易运行；缺点是对高倍率细微差异的表达能力有限。

2. `dinov2_timm`
   这是当前推荐的主方案，采用 `timm` 中的 `vit_small_patch14_dinov2` 作为自监督视觉编码器。DINOv2 对全局结构和语义性纹理具有更强的建模能力，在本项目数据上显著优于手工特征基线。

### 2.3 降维与邻域估计

高维视觉嵌入首先通过 PCA 从原始嵌入空间压缩到 64 维：

$$
z = \mathrm{PCA}(f(I))
$$

降维的作用主要有两点：

- 抑制噪声和冗余维度。
- 提高 kNN 邻域搜索的稳定性。

在降维后的空间中，系统通过 KMeans 生成无监督伪尺度标签，并将不同聚类的中心排序后映射为 `scale proxy`。随后使用加权 kNN 做连续尺度预测：

$$
\hat{s}(x)=\sum_{i \in \mathcal{N}_k(x)} w_i s_i, \quad
w_i = \frac{e^{-d_i}}{\sum_j e^{-d_j}}
$$

其中：

- $d_i$ 是待测样本与邻域样本的距离。
- $s_i$ 是数据库样本对应的伪尺度值。

### 2.4 滑窗映射

连续尺度值最终通过如下函数映射到窗口大小：

$$
W = \mathrm{clip}(\alpha \cdot s^{-\beta}, W_{min}, W_{max})
$$

当前默认参数为：

- $W_{min}=512$
- $W_{max}=1024$
- $\alpha=800$
- $\beta=1.2$

该设计符合“尺度越小，窗口越大；尺度越大，窗口越小”的工程直觉，可直接服务于大图滑窗分割、Mask R-CNN 推理和畴区统计等任务。

## 3. 系统流程

当前工程实现的完整流程如下：

1. 输入显微图像。
2. 将图像按长边统一缩放到 1024，保持长宽比。
3. 通过特征提取器生成全局嵌入。
4. 使用 PCA 将嵌入压缩到 64 维。
5. 使用 KMeans 构建无监督伪尺度库。
6. 基于加权 kNN 预测连续尺度值。
7. 根据尺度值计算滑窗大小。
8. 将窗口大小传递给后续滑窗推理或分割模块。

对应实现中，`evaluate` 命令使用留一法进行外部评估。为避免重复读图和重复提特征导致的计算开销，当前实现已将全部图像嵌入预先缓存，再执行留一法推理，从而显著缩短评估时间。

在当前工程版本中，滑窗推荐不再只依赖固定的经验映射函数，而是新增了基于 `data/images/refer` 的参考视场标定流程：

1. 使用 DINOv2 管线预测 `refer` 图像的尺度归属。
2. 将 `refer` 图像尺寸按预测倍率归一化，得到宽边、短边、长边三类视场代理。
3. 对这些代理使用 `median`、`p75` 或 `max` 等统计量聚合。
4. 根据 `short` 或 `long` 基准，将聚合后的参考视场重新映射回各倍率的推荐滑窗尺寸。

因此，当前系统实际上包含两层输出：

- 连续尺度估计与倍率外评估。
- 基于训练视场参考集的滑窗大小推荐。

在当前版本中，系统还新增了第三类目录级输出：

- 对 `data/images` 下全部图像逐张导出预测倍率、连续尺度和推荐方形滑窗。

## 4. 实现配置

本次实验的关键配置如下：

- 输入数据目录：`data/images`
- 图像总数：43 张
- 倍率类别：`2.5x`、`5x`、`20x`、`50x`、`100x`
- 原始图像样本数：31 张
- SwinIR 图像样本数：12 张
- PCA 维度：64
- kNN 邻居数：5
- 聚类数：6
- 评估方式：Leave-One-Out
- 本次报告中的评估命令使用了 `--disable-multiview`
- 滑窗推荐支持的参考统计量：`median`、`p75`、`max`
- 方形滑窗推荐支持的基准：`short`、`long`
- 批量识别导出命令：`recognize-all`

需要强调的是，本次评估中倍率标签只用于外部评价模型准确性，不参与无监督伪尺度构建过程，因此实验结论仍然反映了“无监督连续尺度估计”的有效性。

## 5. 数据与评估方式

### 5.1 数据分布

数据集包括五种倍率及两类 SwinIR 超分辨率样本：

- `2.5x` 原图 + `2.5x` SwinIR
- `5x` 原图 + `5x` SwinIR
- `20x` 原图
- `50x` 原图
- `100x` 原图

其中 SwinIR 样本仅出现在低倍率 `2.5x` 和 `5x` 类别中，因此 `SwinIR` 鲁棒性评估当前主要反映低倍率条件下的表现。

### 5.2 指标定义

本报告使用以下指标：

- `accuracy`：全样本总体分类准确率。
- `native_accuracy`：原始图像子集准确率。
- `swinir_accuracy`：SwinIR 图像子集准确率。
- `scale_magnification_correlation`：预测连续尺度与真实倍率的相关系数。
- 混淆矩阵：观察不同倍率间的误判模式。

需要说明的是，连续尺度值本身并非物理倍率的直接估计，因此相关系数主要用于反映尺度排序一致性，而不是绝对倍率拟合精度。

对于滑窗推荐部分，本报告新增以下工程指标：

- `aggregated_width_proxy`：参考集宽边视场代理的聚合值。
- `aggregated_height_proxy`：参考集高边视场代理的聚合值。
- `aggregated_short_proxy`：参考集短边视场代理的聚合值。
- `aggregated_long_proxy`：参考集长边视场代理的聚合值。
- `aggregated_square_proxy`：最终用于方形滑窗计算的聚合代理。

这些指标并不是分类精度指标，而是滑窗标定过程中的中间工程量。

## 6. 实验结果

### 6.1 基线模型：轻量鲁棒手工特征

运行配置：

- 提取器：`robust_handcrafted`
- 多视图：关闭

结果如下：

| 指标 | 数值 |
| --- | ---: |
| samples | 43 |
| accuracy | 0.8837 |
| native_samples | 31 |
| swinir_samples | 12 |
| native_accuracy | 0.8387 |
| swinir_accuracy | 1.0000 |
| scale_magnification_correlation | 0.7716 |
| predicted_scale_range | 0.4000 ~ 1.0000 |

基线模型混淆矩阵如下：

| true\\pred | 2.5x | 5x | 20x | 50x | 100x |
| --- | ---: | ---: | ---: | ---: | ---: |
| 2.5x | 12 | 0 | 0 | 0 | 0 |
| 5x | 0 | 12 | 0 | 0 | 0 |
| 20x | 0 | 0 | 6 | 0 | 0 |
| 50x | 0 | 0 | 0 | 4 | 2 |
| 100x | 0 | 0 | 0 | 3 | 4 |

分析：

- `2.5x`、`5x`、`20x` 三类全部识别正确。
- 所有错误都集中在 `50x` 和 `100x` 的互相混淆。
- 说明基线模型足以区分粗粒度尺度，但对相邻高倍率的结构差异刻画不足。

### 6.2 主模型：DINOv2 特征

运行配置：

- 提取器：`dinov2_timm`
- 主干模型：`vit_small_patch14_dinov2`
- 多视图：关闭

结果如下：

| 指标 | 数值 |
| --- | ---: |
| samples | 43 |
| accuracy | 0.9767 |
| native_samples | 31 |
| swinir_samples | 12 |
| native_accuracy | 0.9677 |
| swinir_accuracy | 1.0000 |
| scale_magnification_correlation | 0.6747 |
| predicted_scale_range | 0.2500 ~ 0.9512 |

DINOv2 模型混淆矩阵如下：

| true\\pred | 2.5x | 5x | 20x | 50x | 100x |
| --- | ---: | ---: | ---: | ---: | ---: |
| 2.5x | 12 | 0 | 0 | 0 | 0 |
| 5x | 0 | 12 | 0 | 0 | 0 |
| 20x | 0 | 0 | 6 | 0 | 0 |
| 50x | 0 | 0 | 0 | 6 | 0 |
| 100x | 0 | 0 | 0 | 1 | 6 |

进一步查看逐样本结果，DINOv2 仅有 1 张 `100x` 图像被判为 `50x`，其余 42 张全部正确。

### 6.3 参考视场驱动的滑窗推荐

在完成尺度评估之后，系统新增了 `recommend-windows` 命令，用于根据训练实例分割所使用的 `refer` 图像自动反推各倍率下应采用的滑窗大小。

该过程不是直接使用固定经验值，而是用参考图像的真实像素尺寸来反推“训练视场”。这样做的目标是：

- 让各倍率 patch 的视场与训练参考图尽量一致。
- 避免低倍率窗口过小或高倍率窗口完全脱离训练分布。

当前命令支持两类关键参数：

- `proxy_stat`：`median`、`p75`、`max`
- `square_basis`：`short`、`long`

其中：

- `median` 表示用参考视场的中位数做标定，更保守、更稳健。
- `max` 表示用参考视场的最大值做标定，保证 patch 不小于最大参考视场，但窗口会显著变大。
- `short` 表示方形 patch 按参考图短边视场匹配。
- `long` 表示方形 patch 按参考图长边视场匹配，更适合“不要明显小于 refer 视场”的场景。

本次最终保留并展示的一组窗口来自 `median + long` 配置，其结果如下：

| 倍率 | 推荐矩形窗口 | 推荐方形窗口 | 对齐后方形窗口 |
| --- | --- | ---: | ---: |
| 2.5x | 204 x 136 | 204 | 192 |
| 5x | 409 x 272 | 409 | 416 |
| 20x | 1636 x 1088 | 1636 | 1632 |
| 50x | 4090 x 2720 | 4090 | 4096 |
| 100x | 8180 x 5440 | 8180 | 8192 |

这些结果与 [artifacts/window_recommendations_median_long.csv](artifacts/window_recommendations_median_long.csv) 中的实际输出一致。

### 6.4 全量图像识别导出

在滑窗推荐之外，当前版本新增了 `recognize-all` 命令，用于对指定根目录下的全部图像逐张执行识别并导出结构化结果。该命令的输出逻辑如下：

1. 先用 `dinov2_timm` 管线预测每张图像的连续尺度。
2. 再根据训练集类别中位尺度把连续尺度映射为预测倍率标签。
3. 最后按当前选定的滑窗推荐策略，为该标签分配推荐方形窗口大小。

本次使用的配置为：

- 输入目录：`data/images`
- 特征提取器：`dinov2_timm`
- 滑窗推荐策略：`median + long`

最终导出文件为 [artifacts/recognize_all_median_long.csv](artifacts/recognize_all_median_long.csv)，共覆盖 57 张图像。输出字段包括：

- `image`
- `predicted_label`
- `predicted_scale`
- `recommended_square_window`
- `proxy_stat`
- `square_basis`

这说明当前系统已经不仅能用于评估，还可以对目录级数据直接产出“识别结果 + 推荐滑窗”的可消费结果。例如：

- `20x` 图像被识别为 `20x`，对应推荐方形滑窗为 `1632`。
- `50x` 图像被识别为 `50x`，对应推荐方形滑窗为 `4096`。
- `5x` 图像被识别为 `5x`，对应推荐方形滑窗为 `416`。

此外，`recognize-all` 还会覆盖 `refer` 目录中的参考图像，因此同一套接口可以同时处理原图、SwinIR 图像和训练参考图像。

### 6.5 Patch 可视化验证

为验证推荐窗口是否与不同倍率图像的视场相匹配，系统新增了 `visualize-windows` 命令。当前版本的可视化逻辑有两个重要特点：

1. 只从 `data/images` 各倍率目录中选择代表图，不再混入 `refer` 目录图像。
2. 对于同一倍率下的原图与 SwinIR 图像，分别输出 `native` 和 `swinir` 两套可视化结果。

这意味着：

- `2.5x` 与 `5x` 会各自生成 `native` 和 `swinir` 两种 patch / preview。
- `20x`、`50x`、`100x` 当前只有 `native`，因为数据集中没有对应的 `sr` 目录。

以 `median + long` 配置为例，当前生成的可视化摘要如下：

| 倍率 | 变体 | 图像尺寸 | 推荐方形窗口 | 模式 |
| --- | --- | --- | ---: | --- |
| 2.5x | native | 4096 x 2724 | 192 | patch |
| 2.5x | swinir | 4096 x 2724 | 192 | patch |
| 5x | native | 4096 x 2724 | 416 | patch |
| 5x | swinir | 4096 x 2724 | 416 | patch |
| 20x | native | 1024 x 681 | 1632 | preview_only |
| 50x | native | 1024 x 681 | 4096 | preview_only |
| 100x | native | 1024 x 681 | 8192 | preview_only |

这里 `preview_only` 的含义不是失败，而是推荐窗口已经大于当前样本图尺寸，因此系统只能输出：

- 最大可裁中心 patch
- 带推荐窗口框的预览图

这一步反映的恰恰是当前窗口推荐与现有小图样本之间的视场关系：高倍率推荐窗口非常大，而用于展示的局部样本图尺寸不足，无法完整承载该视场。

## 7. 对比分析

### 7.1 精度对比

| 模型 | accuracy | native_accuracy | swinir_accuracy |
| --- | ---: | ---: | ---: |
| 手工特征基线 | 0.8837 | 0.8387 | 1.0000 |
| DINOv2 | 0.9767 | 0.9677 | 1.0000 |

结论：

- DINOv2 相比基线总体准确率提升了 0.0930。
- DINOv2 在原始图像上的准确率提升了 0.1290。
- 两个模型在现有 SwinIR 子集上均达到 1.0000，但当前 SwinIR 样本仅覆盖低倍率，因此该结论仍需更多高倍率 SwinIR 数据验证。

### 7.2 误差模式对比

基线模型的 5 个错误全部来自 `50x` 与 `100x` 互相混淆，这说明手工统计特征在高倍率下对全局结构和细粒度纹理模式的区分能力不够。

DINOv2 将这一问题显著缓解，仅剩 1 个错误样本，说明自监督视觉表征更能稳定捕捉高倍率显微图像中的结构差异，也更符合原始工程方案中“使用 DINOv2 作为主特征抽取器”的设计判断。

### 7.3 关于连续尺度相关性

一个看似反直觉的现象是：DINOv2 的分类准确率更高，但 `scale_magnification_correlation` 低于基线。

这并不意味着 DINOv2 更差，原因在于当前连续尺度是通过无监督聚类后再映射得到的伪尺度值，而不是直接拟合真实倍率的回归量。因此：

- 更高的分类正确率意味着类别可分性更强。
- 较低的相关系数说明当前伪尺度数值本身还不是理想的“连续物理尺度”。

换句话说，当前系统已经证明了“尺度空间中的类别排序与邻域结构”是有效的，但若希望进一步提升连续性与物理解释性，后续需要优化尺度代理构造方式。

## 8. 工程价值

从工程视角看，本项目已经具备以下实用价值：

1. 可以从图像本身估计尺度，而不依赖比例尺或显式倍率元数据。
2. 可以将尺度估计结果直接映射为滑窗大小，服务于大图滑窗推理。
3. 在当前数据上，DINOv2 版本已经达到 0.9767 的总体准确率，具备较强实用性。
4. 在当前低倍率 SwinIR 子集上，系统表现稳定，没有出现明显退化。
5. 当前工程版本已经支持基于 `refer` 训练视场的自动窗口推荐，不再依赖单一固定窗口。
6. 当前工程版本已经支持 `native` / `swinir` 双分支 patch 可视化验证，便于直接检查超分辨率图像上的窗口适配效果。
7. 当前工程版本已经支持对目录下全部图像批量导出识别结果与推荐滑窗，可直接接入下游滑窗推理流程。

因此，该系统已经可以作为“自适应滑窗大小估计模块”接入后续分割或检测流程，而不需要再依赖固定窗口策略。

## 9. 当前局限

尽管结果较好，但目前仍存在以下局限：

1. SwinIR 样本只覆盖 `2.5x` 与 `5x`，尚不能充分验证高倍率超分辨率图像上的鲁棒性。
2. 连续尺度值由聚类伪标签诱导得到，仍然存在离散化倾向，物理解释性有限。
3. 评估规模较小，仅 43 张图像，仍需更大样本验证稳定性。
4. 当前主要验证的是尺度分类可分性与排序能力，还没有与实际滑窗分割精度做联动评估。
5. 当前用于展示的 `20x`、`50x`、`100x` 样本图尺寸较小，因此窗口可视化只能体现“推荐窗口相对样本图的大小关系”，还不能完全等价于真实大图滑窗效果。
6. `recognize-all` 当前输出的推荐窗口仍然继承自无监督伪尺度体系，因此其物理解释性仍受 proxy 构造方式约束。

## 10. 后续优化建议

建议后续从以下几个方向继续推进：

1. 将 DINOv2 作为默认主干，手工特征仅保留为轻量回退方案。
2. 引入更连续的尺度代理，例如频率代理、结构尺度代理或弱监督回归代理。
3. 增加高倍率 SwinIR 数据，单独评估 `50x` 与 `100x` 的超分辨率鲁棒性。
4. 将当前尺度估计模块与滑窗分割系统联动，评估不同窗口策略对实例分割或目标统计结果的影响。
5. 在工程加速层面，引入 FAISS 替代当前的标准最近邻搜索，以支持更大规模图库。
6. 将窗口推荐进一步参数化，例如增加额外放大系数，以更精细地控制 patch 视场与训练参考图之间的匹配程度。
7. 为 `recognize-all` 增加更丰富的输出字段，例如矩形窗口大小、近邻距离统计以及是否为 SwinIR 图像的标记。

## 11. 结论

本项目已经完成了从方案到工程实现、再到真实数据验证的闭环。

实验表明：

- 无监督连续尺度估计方案是可行的。
- DINOv2 是当前最有效的特征表征方式。
- 在 43 张真实样本上的留一法评估中，DINOv2 达到 0.9767 的总体准确率，仅错 1 张样本。
- 当前系统能够稳定区分低倍率与高倍率图像，并且在现有 SwinIR 子集上保持了 1.0000 的准确率。
- 当前系统已经实现了参考视场驱动的滑窗推荐，并支持 `median/max` 与 `short/long` 两类策略组合。
- 当前系统已经支持将原图与 SwinIR 图像分开做 patch 可视化验证，能够直接检查不同输入域上的窗口适配情况。
- 当前系统已经支持对 `data/images` 下全部图像进行批量识别，并同步导出 `median + long` 模式下的推荐方形滑窗。

综合来看，该方法已经证明了“将倍率问题转化为连续视觉尺度建模问题”是合理且有效的，并具备进一步接入实际滑窗分割工程系统的基础。